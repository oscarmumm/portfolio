import { Link } from "react-router";

export const ProjectsList = () => {
    return (
        <div>
            <h2>ProjectsList</h2>
            <Link to="/projects">View all projects </Link>
        </div>
    );
};
